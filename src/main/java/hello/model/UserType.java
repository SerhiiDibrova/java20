```java
package hello.model;

public enum UserType {
    USER, ADMIN, ALL
}
```

Note: Since the original code is already quite simple and doesn't use any features that are deprecated in Java 20, there's no need to make any changes. The provided code is compatible with Java 20 standards.