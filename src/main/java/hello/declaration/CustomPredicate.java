```java
package hello.declaration;

public interface CustomPredicate<T> {
    boolean test(T t);
}
```

Note: Since the provided Java file is an interface and does not contain any implementation details, there's no need to update it for Java 20. The code remains the same as the original version.