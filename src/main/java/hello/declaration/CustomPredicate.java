```java
package hello.declaration;

public interface CustomPredicate<T> {
    boolean test(T t);
}
```

Note: Since the original code is already quite simple and does not contain any deprecated features, there's no need to make significant changes. The provided Java 8 code is compatible with Java 20 standards.